spring:
  application:
    name: search-index-manager

logging:
  level:
    com.izenkyt.searchindexmanager: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
  structured:
    format:
      file:
